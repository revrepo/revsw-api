"use strict";

var mocha       = require('mocha');
var chai        = require('chai');
var _           = require('lodash');
var LoggerAudit = require('revsw-audit');

describe('init', function () {
  var failureObject = {};
  var failureObject2  = {
    mongodb : {
      db         : 'some config',
      collection : 'some collection'
    }
  };
  var failureObject3 = {
    mongodb : {
      collection : 'some collection'
    },
    file : {
      timestamp : 'some boolean'
    }
  };
  var correctObject = {
    mongodb : {
      db         : 'some config',
      collection : 'some collection'
    },
    file : {
      filename  : 'some filename',
      timestamp : 'some boolean'
    }
  }
  it('Test initialization', function () {
    console.log(LoggerAudit);
  })
});
